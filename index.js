import { render, Component } from 'preact'
import Base from './src/Base'
import Stage from './src/Stage'
import Graphics from './src/Graphics'
import RenderTexture from './src/RenderTexture'

export const Container = ({}) => null
export const ParticleContainer = ({}) => null
export const Text = ({}) => null
export const BitmapText = ({}) => null
export const Sprite = ({}) => null
export const AnimatedSprite = ({}) => null
export const TilingSprite = ({}) => null
export const Rope = ({}) => null

export
{
    Base,
    Stage,
    Graphics,
    RenderTexture,
}
